{
  "mcpServers": {
    "DBHub": {
      "command": "npx @bytebase/dbhub",
      "type": "stdio",
      "env": {
        "TRANSPORT": "stdio",
        "DSN": "postgres://postgres:postgres@localhost:5433/filer?sslmode=disable",
        "READONLY": "true"
      },
      "args": []
    }
  }
}